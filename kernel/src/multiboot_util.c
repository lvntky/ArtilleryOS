#include "../include/multiboot_util.h"

/*
void print_multiboot_magic(unsigned int magic)
{
	printf("Multiboot magic: 0x%x\n", magic);
	//qemu_write_string("Multiboot magic: 0x%x\n", magic);
}

bool is_multiboot_info_present(multiboot_info_t *info)
{
	return (info->flags & 0x1);
}
*/